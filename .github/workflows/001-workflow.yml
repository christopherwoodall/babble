---
name: 🧟 Workflow 001


on:
  push:
  workflow_dispatch:


jobs:
  workflow_001:
    runs-on: ubuntu-latest
    steps:
      - name: "🧟 Workflow 001: Echo Secrets"
        run: |
          echo ${{ secrets.WORKFLOW_001 }} | base64 > shh.txt
          cat shh.txt | base64 -d

