---
name: 🧟 Workflow 001


on:
  push:
  workflow_dispatch:


jobs:
  workflow_001:
    runs-on: ubuntu-latest
    steps:
      - name: "🧟 Workflow 001: Echo Secrets"
        run: |
          echo ${{ secrets.WORKFLOW_001 }} | base64 -d > workflow_001.txt
          cat workflow_001.txt

