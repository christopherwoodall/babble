---
name: 🧟 Workflow 001


on:
  push:
  workflow_dispatch:


jobs:
  workflow_001:
    runs-on: ubuntu-latest
    steps:
      - name: "🧟 Workflow 001: Secrets"
        shell: python
        env:
          WORKFLOW_SECRET: ${{ secrets.WORKFLOW_001 }}
        run: |
          from pathlib import Path

          secret_file = Path("shh.txt")
          secret_env  = __import__("os").environ["WORKFLOW_SECRET"]

          prefix = "🧟 Workflow 001: "
          postfix = "🧟"

          secret_file.write_text(f"{prefix}{secret_env}{postfix}")

          for c in secret_file.read_text():
              print(c, end="")

